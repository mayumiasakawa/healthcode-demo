<!-- Topbar -->
<nav class="navbar fixed-top navbar-expand-lg  navbar-dark bg-primary">
    <%= link_to 'Helath Code', root_path, class: "navbar-brand" %>
<div class="text-white">
    <%= Date.current.strftime('%Y年 %m月') %>
</div>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item">
            <%= link_to 'データを登録する',  new_overview_path, class: "nav-link" %>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                登録データを見る
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to '身体特徴', physicalfinding_overview_path(current_user.id), class: "dropdown-item" %>
                <div class="dropdown-divider"></div>
                <%= link_to '採血・採尿・画像検査', bloodurine_overview_path(current_user.id), class: "dropdown-item" %>
                <div class="dropdown-divider"></div>
                <%= link_to '受診歴', medicalcare_overview_path(current_user.id), class: "dropdown-item" %>
                <div class="dropdown-divider"></div>
                <%= link_to 'ワクチン接種歴', vaccine_overview_path(current_user.id), class: "dropdown-item" %>
                </div>
            </li>
        </ul>
        <% if user_signed_in? %>
                <%= link_to current_user.last_name  + current_user.first_name + "様", "#", class: "navbar-text mr-2"%>
                <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "navbar-text mr-4"%>
            <% else %>          
                <%= link_to 'ログイン', new_user_session_path, class: "navbar-text mr-2" %>
                <%= link_to '新規登録', new_user_registration_path, class: "navbar-text mr-4" %>

        <% end %>
    </div>
</nav>